<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.isc.laraproyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FRAME_ACTIVITY7"></activity>
        <activity android:name=".FRAME_ACTIVITY6" />
        <activity android:name=".FRAME_ACTIVITY5" />
        <activity android:name=".FRAME_ACTIVITY4" />
        <activity android:name=".FRAME_ACTIVITY3" />
        <activity android:name=".FRAME_ACTIVITY2" />
        <activity android:name=".FRAME_ACTIVITY" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>